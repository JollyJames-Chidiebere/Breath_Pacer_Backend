[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
# Don't run migrations here - they need DATABASE_URL from runtime

[start]
cmd = "gunicorn Breath_Pacer_Backend.wsgi:application --bind 0.0.0.0:$PORT"
